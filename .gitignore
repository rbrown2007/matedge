# Environment variables — never commit these
.env
.env.local
.env.production

# Node modules
node_modules/

# OS files
.DS_Store
Thumbs.db

# Firebase config — contains API keys, never commit
firebase-config.js
